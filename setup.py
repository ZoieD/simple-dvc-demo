from setuptools import setup, find_packages

setup(
    name = "src", 
    version = "0.0.1", 
    description = "its a wine Q packages",
    author = "dou zi",
    packages=find_packages(),
    license = "MIT"
)

